server:
  port: 8080

spring:
  application:
    name: poetry-rag-backend
  datasource:
    url: jdbc:mysql://localhost:3306/poetry_rag?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    serialization:
      write-dates-as-timestamps: false

ai:
  service:
    url: http://localhost:8000/ai/api/v1/generate/async
